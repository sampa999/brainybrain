<Window x:Class="WebcamWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Webcam Demo" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <ComboBox x:Name="comboCameras" Width="200"/>
            <Button x:Name="btnStart" Content="Start" Margin="10 0" Click="btnStart_Click"/>
            <Button x:Name="btnCapture" Content="Capture" Click="btnCapture_Click"/>
        </StackPanel>

        <Image x:Name="imagePreview" Grid.Row="1" Stretch="Uniform"/>

    </Grid>
</Window>
